{
  "name": "omni-agent",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter api start:dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "typescript": "^5.9.3"
  }
}
