# Base URL of the OmniAgent NestJS API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: repository link shown in the navbar and landing page.
NEXT_PUBLIC_GITHUB_URL=https://github.com/Nowaj36/omni-agent
